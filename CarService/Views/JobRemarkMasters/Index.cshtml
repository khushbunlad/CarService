@model IEnumerable<CarService.Models.Entities.TblJobRemarkMaster>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FldRemarkTitle)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FldRemarkTitle)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FldRemarkId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FldRemarkId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FldRemarkId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

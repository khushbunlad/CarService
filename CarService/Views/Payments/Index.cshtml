@model IEnumerable<CarService.Models.Entities.TblPayment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FldJobId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FldPaidAmount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FldPaymentMode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FldPaymentDatetime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FldJobId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FldPaidAmount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FldPaymentMode)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FldPaymentDatetime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FldPaymentId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FldPaymentId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FldPaymentId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
